'use strict';

/**
 * landing-advertisement service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::landing-advertisement.landing-advertisement');
