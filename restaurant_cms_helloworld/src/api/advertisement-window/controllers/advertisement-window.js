'use strict';

/**
 *  advertisement-window controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::advertisement-window.advertisement-window');
