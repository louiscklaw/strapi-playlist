'use strict';

/**
 * advertisement-window router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::advertisement-window.advertisement-window');
