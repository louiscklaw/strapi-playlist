'use strict';

/**
 * advertisement-window service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::advertisement-window.advertisement-window');
